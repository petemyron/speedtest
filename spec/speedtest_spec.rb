require 'spec_helper'

describe Speedtest do
  it 'has a version number' do
    expect(Speedtest::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
